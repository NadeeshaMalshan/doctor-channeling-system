# Loaded by Create React App for `npm start` only (not for `npm run build`).
# To use a local AI server instead, create `.env.development.local` (gitignored) with:
#   REACT_APP_AI_API_URL=http://localhost:8000
REACT_APP_AI_API_URL=https://nadeeshamalshan-nccecareai.hf.space
